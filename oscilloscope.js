//later
var analyser = audioCtx.createAnalyser();
var contentWidth = document.getElementById('allIn').offsetWidth;
//var oscilloscope = new Oscilloscope(audioCtx, analyser, contentWidth, 150);
//masterGain.connect(oscilloscope.analyser);